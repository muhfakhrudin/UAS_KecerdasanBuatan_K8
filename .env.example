# Copy this file to .env for local development. Render sets these as real
# environment variables in its dashboard instead — .env is never deployed
# (it's git-ignored).

SECRET_KEY=django-insecure-change-me-for-local-dev-only
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
